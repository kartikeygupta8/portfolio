name: Keep Render service alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: curl -sf -o /dev/null https://kartikeygupta.co || curl -sf -o /dev/null https://portfolio-kwla.onrender.com
